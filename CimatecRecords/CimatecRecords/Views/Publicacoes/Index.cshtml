@model IEnumerable<CimatecRecords.Models.Publicacao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Letra.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Letra.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PK_Publicacao }) |
            @Html.ActionLink("Details", "Details", new { id=item.PK_Publicacao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PK_Publicacao })
        </td>
    </tr>
}

</table>
